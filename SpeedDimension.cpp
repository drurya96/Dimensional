#include "SpeedDimension.h"

//using namespace Dimension;

namespace Dimension
{








   /*
   Speed operator/(const Length& length, const Time& time)
   {
      // determine unit type
      SpeedUnit unit = SpeedUnits::FeetPerSecond;
      return Speed(length.value / time.value, unit);
   }
   */

   /*

   Length operator*(const Speed& speed, const Time& time)
   {

   }
   */

   /*
   Time operator/(const Length& length, const Speed& speed)
   {
      // determine unit type
      SpeedUnit unit = SpeedUnits::FeetPerSecond;
      return Speed(length.value / speed.value, unit);
   }
   */
   

   /*
   Speed operator/(const BaseDimension& num, const BaseDimension& den)
   {
      // determine unit type
      //SpeedUnit unit = SpeedUnits::FeetPerSecond;
      //return Speed(length.value / time.value, unit);
   }
   */
}
//Time::Time(double value, TimeUnit unit):
//   value(value),
//   unit(unit)
//{

//}

//bool Time::RegisterUnit(TimeUnit unit)
//{
   //return true;
//}

/*
TimeUnit::TimeUnit(std::string name, void* conversionFunctor)
{

}
*/