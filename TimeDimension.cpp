#include "TimeDimension.h"

using namespace Dimension;

/*
double Time::GetVal(TimeUnit<>* getUnit)
{
   if (getUnit == unit)
   {
      return value;
   }
   else
   {
      return unit->conversions[getUnit->unitName](this->value);
   }
}
*/

/*
Time::operator BaseDimension<TimeUnit<>>()
{
   return BaseDimension<TimeUnit<>>();
}
*/

/*
double Inverse_Time::GetVal(Inverse_TimeUnit* getUnit)
{
   if (getUnit == unit)
   {
      return value;
   }
   else
   {
      return unit->conversions[getUnit->unitName](this->value);
   }
}
*/

//Time::Time(double value, TimeUnit unit):
//   value(value),
//   unit(unit)
//{

//}

//bool Time::RegisterUnit(TimeUnit unit)
//{
   //return true;
//}

/*
TimeUnit::TimeUnit(std::string name, void* conversionFunctor)
{

}
*/